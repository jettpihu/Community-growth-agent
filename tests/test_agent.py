def test_reversible_failure():
    """Intentional reversible failing test for PR/CI validation.

    This test is intentionally failing so you can validate CI and PR workflows.
    Remove or update this test before merging into the main branch.
    """
    assert False, "Intentional failing test: remove before merging"
